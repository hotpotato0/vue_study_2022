* Vue
- MVVM 패턴의 뷰모델(ViewModel) 레이어에 해당하는 화면(View)단 라이브러리
- DOM Lisenters : 화면단에서 이벤트 발생했을 때 자바스크립트 수행
- Data Bindings : 자바스크립트 데이터가 바뀌었을 때 이를 통해서 화면에 반영함

* 뷰 인스턴스
뷰로 개발할 때 필수로 생성해야함
new Vue();
el
templete
data
methods
created
watch

* 함수 앞에 대문자 : 생성자 함수를 의미

<컴포넌트 통신>
* 뷰 컴포넌트는 각각 고유한 데이터 유효범위를 갖는다
- 프롭스 속성 : 상위에서 하위로 데이터를 내려줌
- 이벤트 발생 : 하위에서 상위로 데이터를 올려줌

* 검색 : mdn 검색대상

